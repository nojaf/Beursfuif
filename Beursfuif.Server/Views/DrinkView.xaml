<UserControl  x:Class="Beursfuif.Server.Views.DrinkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:m="clr-namespace:Beursfuif.BL;assembly=Beursfuif.BL"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                     xmlns:interact="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:mvvm= "clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Extras.WPF4"
             mc:Ignorable="d" 
             DataContext="{Binding Drink,Source={StaticResource Locator}}"
             d:DesignHeight="715" MinHeight="715" d:DesignWidth="751" MinWidth="751"
             x:Name="drinkView">
    <Grid Background="White">
    	<VisualStateManager.VisualStateGroups>
    		<VisualStateGroup x:Name="HideShow">
    			<VisualState x:Name="FadeIn">
    				<Storyboard>
    					<DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="userControl">
    						<EasingDoubleKeyFrame KeyTime="0" Value="0"/>
    						<EasingDoubleKeyFrame KeyTime="0:0:0.5" Value="1"/>
    					</DoubleAnimationUsingKeyFrames>
    				</Storyboard>
    			</VisualState>
    			<VisualState x:Name="FadeOut">
    				<Storyboard>
    					<DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="userControl">
    						<EasingDoubleKeyFrame KeyTime="0" Value="1"/>
    						<EasingDoubleKeyFrame KeyTime="0:0:0.5" Value="0"/>
    					</DoubleAnimationUsingKeyFrames>
    				</Storyboard>
    			</VisualState>
    		</VisualStateGroup>
    	</VisualStateManager.VisualStateGroups>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="79*" />
            <ColumnDefinition Width="31*" />
        </Grid.ColumnDefinitions>
        <ListView ItemsSource="{Binding Drinks}" AlternationCount="2" BorderBrush="Transparent" BorderThickness="0" Background="Transparent">
            <ListView.View>
                <GridView ColumnHeaderContainerStyle="{StaticResource HeaderColumnText}">
                    <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Naam">
                    </GridViewColumn>
                    <GridViewColumn Header="Image">
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <Viewbox>
                                <Image Source="{Binding ImageUri}" Height="50" />
                            </Viewbox>
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding InitialPrice}" Header="Startprijs"></GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding MiniumPrice}" Header="Minimumprijs"></GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding MaximumPrice}" Header="Maximumprijs"></GridViewColumn>
                    <GridViewColumn Header="Beschikbaar">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Path=Available, Mode=TwoWay}"></CheckBox>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Verwijderen">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource RemoveButtonIcon}" 
                                        CommandParameter="{Binding Id}"
                                        Command="{Binding Path=DataContext.RemoveDrink,ElementName=drinkView}" Height="25" HorizontalAlignment="Center"></Button>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
